apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: vault-storage-class
provisioner: kubernetes.io/aws-ebs
parameters:
  type: gp3  # Updated to gp3 for Vault as well
reclaimPolicy: Retain
volumeBindingMode: WaitForFirstConsumer
